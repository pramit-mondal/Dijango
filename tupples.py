#Tuple is also similar to list but contains immutable objects. Tuple processing is faster than List.
#1.	Tuple is immutable.
#2.Tuple iteration is faster.
#3.Tuple is useful for readonly operations like accessing elements.
three_numbers=tuple((1,2,'home',True,3))
string=('home','land','earth')
bool=(True,False,True)
print(three_numbers)
